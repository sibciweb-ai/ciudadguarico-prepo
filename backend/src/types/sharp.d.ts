declare module 'sharp' {
  // Minimal typing to satisfy the TS compiler in a CommonJS project
  // You can replace `any` with more specific types if needed.
  const sharp: any;
  export default sharp;
}
